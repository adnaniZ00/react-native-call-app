<?xml version="1.0" encoding="utf-8"?>
<!--
    We are using RelativeLayout instead of ConstraintLayout to avoid a
    stubborn Gradle dependency issue. RelativeLayout is built-in and doesn't
    require an extra library.
-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#2E3B4E"
    android:padding="16dp">

    <TextView
        android:id="@+id/callerNameText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="128dp"
        android:text="Caller Name"
        android:textColor="@android:color/white"
        android:textSize="34sp" />

    <TextView
        android:id="@+id/callTypeText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/callerNameText"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:text="Incoming Video Call..."
        android:textColor="@android:color/darker_gray"
        android:textSize="18sp" />

    <Button
        android:id="@+id/declineButton"
        android:layout_width="120dp"
        android:layout_height="60dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="48dp"
        android:layout_marginBottom="128dp"
        android:backgroundTint="#E91E63"
        android:text="Decline" />

    <Button
        android:id="@+id/acceptButton"
        android:layout_width="120dp"
        android:layout_height="60dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="48dp"
        android:layout_marginBottom="128dp"
        android:backgroundTint="#4CAF50"
        android:text="Accept" />

</RelativeLayout>
